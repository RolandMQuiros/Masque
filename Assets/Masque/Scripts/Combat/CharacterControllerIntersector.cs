using UnityEngine;
using System.Collections;

/// <summary>
/// Pushes other Character Controllers out of collisions with each other
/// </summary>
[RequireComponent(typeof(CharacterController))]
public class CharacterControllerIntersector : MonoBehaviour {

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
